syntax = "proto3";

package pubsub;

service PublisherService {
    rpc Subscribe (Topic) returns (stream Message) {}
}

message Topic {
    string name = 1;
}

message Message {
    string msg = 1;
    string timestamp = 2;
}